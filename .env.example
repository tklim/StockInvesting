# The only browser-visible environment variable used by the public site.
# Set this in Vercel for each environment; do not put provider keys here.
VITE_CONVEX_URL=https://your-production-deployment.convex.cloud
